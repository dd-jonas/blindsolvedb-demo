import { Alert } from '#components';

export const Account = () => {
  return (
    <div className="account">
      <Alert warning>Account settings are disabled for this demo.</Alert>
    </div>
  );
};
